# KnowFlow V1 验收记录（2026-08-11）

> 本记录只包含合成测试数据、非敏感环境标识和命令结果。未记录 API Key、主密钥、私人聊天、
> 知识正文或完整 Prompt。

## 版本与环境

- 验收日期：`2026-08-11`
- 起始 / 当前 HEAD：`649f99c2daf25dabeefe9ac736b6e0cb91c2afb7`
- 分支：`feature/document-upload-local-storage`
- 工作树状态：`dirty`；Phase 09 实现尚未提交，因此本记录不能替代可复现的验收 commit
- 操作系统 / CPU：macOS `26.5.2 (25F84)` / `arm64`
- Java / Node.js / npm：`21.0.11` / `v26.0.0` / `11.12.1`
- Docker / Compose：`29.4.3` / `v5.1.3`
- MySQL / Redis / RabbitMQ / Qdrant：`8.4` / `7-alpine` / `3.13-management` / `1.12.4`
- 浏览器 / Playwright：Chromium / `1.62.1`

## 模型标识（不含 Secret）

- ChatModel Config：E2E 运行时通过正式设置页创建，OpenAI-compatible local stub
- Utility Model Config：E2E 运行时通过正式设置页创建并测试，OpenAI-compatible local stub
- Embedding：local stub / `text-embedding-v4` / `1024`
- 确定性 Stub 版本：本 Phase 09 dirty working tree 中的 `scripts/model-stub.py`
- 真实 Chat / Utility / Embedding：`NOT_RUN`；本次未提供可记录的受控 config ID，且默认验收不读取真实 Secret

## 自动化验收

| 命令 | 结果 | 时间 | 备注 |
|---|---|---|---|
| `sh scripts/verify-fast.sh` | PASS | `verify-all` 内执行 | 后端 178/178；前端 34/34；OpenAPI drift、typecheck、format、build 通过 |
| `sh scripts/verify-integration.sh` | PASS | 2m32s | Testcontainers 集成测试 86/86 |
| `sh scripts/verify-e2e.sh` | PASS | 41.1s | Chromium 5/5；专用 Compose project 与临时文件目录均已清理 |
| `sh scripts/verify-failure-drills.sh` | PASS | 约 1m20s | 定向后端 17/17，failure-path Playwright 2/2 |
| `sh scripts/verify-all.sh` | PASS | 2026-08-11 23:30 开始 | fast、Secret 扫描、integration、E2E 全部通过 |

补充验证：

- `docker compose config --quiet`：PASS
- `docker compose build app frontend`：PASS；更新 lockfile 后又单独重建 frontend，`npm ci` 报告 0 vulnerabilities
- `npm --prefix frontend audit`：PASS，0 vulnerabilities
- `git diff --check` 与 Phase 09 Shell 脚本 `sh -n`：PASS
- 根 Compose 运行态 smoke：`NOT_RUN`；检测到已有、已停止的开发容器，未启动或替换，避免影响现有本地数据

断言矩阵结果：

| 范围 | 结果 | 失败 ID / 证据 |
|---|---|---|
| C-01..C-06 | PASS | `conversation-loop.spec.ts` + Conversation/Extraction/RAG 集成测试 |
| N-01..N-06 | PASS | `note-loop.spec.ts` + Document/Index/RAG 集成测试 |
| X-01..X-04 | PASS | OpenAPI drift、owner 隔离测试、E2E guard、tracked-secret scan |
| F-01..F-04 | PASS | Consumer/Recovery IT + `failure-paths.spec.ts` |

## 两个正式前端闭环

### 对话沉淀闭环

- 结果：PASS
- 标识：由隔离 E2E 数据库在运行时生成，执行后随专用数据库清理
- 调用链：设置页创建并测试模型 → 两轮正式聊天/SSE → Extraction Task → Candidate 编辑/确认 →
  KnowledgeItem 索引 → 新会话 Router/RAG → Sources/cited 断言
- 证据：`conversation-loop.spec.ts` 1/1；C-01..C-06 全部通过

### Manual Note / Upload 闭环

- 结果：PASS
- 标识：由隔离 E2E 数据库在运行时生成，执行后随专用数据库清理
- 调用链：Knowledge 页创建 KnowledgeBase/Manual Note 或上传 Markdown → Document Parse / Index Task →
  Item Detail / 下载入口 → 新会话 Router/RAG → Sources、KB filter 断言
- 证据：`note-loop.spec.ts` 2/2；N-01..N-06 全部通过

## 失败与恢复演练

| 场景 | MySQL 终态 | Retry / DLQ | 前端可见性 | 结果 |
|---|---|---|---|---|
| Extraction 失败 | Task `FAILED`，snapshot 失败 | 手动 Retry 新建关联 Task 并重新绑定 snapshot | Processing 可重试 | PASS |
| Document Processing 失败 | Task / File / Item 保持一致的失败状态与错误码 | 终态错误不盲目重试 | Processing / Item Detail 可见 | PASS |
| Embedding 失败 | 3 次重试耗尽后 Task / Item `FAILED` | DLQ task ID 一致；手动 Retry 后 `SUCCEEDED` / `INDEXED` | Processing 页 Retry | PASS |
| Qdrant Index 失败 | Task / Item `FAILED` | 可重试错误退避，耗尽进入 DLQ | 稳定错误码可见 | PASS |
| Consumer 最大重试 | Task `FAILED` | 重试计数与 DLQ 一致 | Processing 显示失败 | PASS |
| PENDING / PROCESSING 重启恢复 | PENDING 重发；过期 lease 恢复；有效 lease 不误发 | 恢复扫描幂等 | 任务状态可查询 | PASS |
| 用户手动 Retry | 新 Task 记录 `retryOfTaskId`，领域状态重置 | commit 后发布新 taskId | Processing 页可操作 | PASS |
| SSE 中断 / 停止 / 重新生成 | `FAILED` / `CANCELLED` / 新 active attempt | 均释放 conversation generation slot | Chat 页可继续发送/重新生成 | PASS |

## 真实模型受控验收

- 结果：`NOT_RUN`
- 未运行原因：真实 Provider 凭据和 config ID 未进入本次受控执行上下文；为避免泄露 Secret 或产生未授权费用，
  没有调用云端 Chat、Utility 或 Embedding
- DeepSeek / Qwen 兼容性观察：`NOT_RUN`
- Router / Retrieval / Citation 质量观察：`NOT_RUN`；确定性 stub 只证明系统契约和数据流，不等同于真实模型质量

## GitHub Actions

- Workflow / Run URL：`NOT_RUN`
- 被验证 commit：`NOT_RUN`；Phase 09 工作树尚未提交或推送
- 结果：`NOT_RUN`
- 未运行原因：本任务未获授权提交、推送或触发远端 workflow；本地 `verify-all` 已验证同一脚本入口

## 已知限制与非阻断项

- 前端 ESLint：0 errors、8 个现有格式 warning。
- Vite 生产构建：主 chunk 约 769 kB，保留大于 500 kB 的性能 warning。
- 根 Compose 镜像与静态配置已验证，但为保护已有本地容器/数据，没有执行根栈运行态 smoke。
- 当前结果基于 dirty working tree；提交后应在该 commit 上重新运行 GitHub Actions。
- 仍待真实 Provider 受控验收、最终独立 Review 和 Owner 手工验收。
- V1 明确排除：应用内认证、公网裸露部署、PDF/Word、MinIO、MCP、Agent、GraphRAG、Hybrid Search、Dashboard。

## 结论

- V1 候选结论：`PARTIAL`
- 确定性工程验收：PASS
- 尚未关闭的发布门禁：真实 Provider 受控验收、远端 GitHub Actions、最终独立 Review、Owner 手工验收
- Owner 验收：pending
